name: realtime_tts_stt
description: Realtime STT + TTS Flutter web client for OpenAI Realtime
publish_to: "none"
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  audioplayers: ^5.2.1
  provider: ^6.0.5
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
  assets:
    - assets/voices/
