### Dockerfile neutralized per user request to remove deployment artifacts that required additional cloud permissions.
### If you want to re-enable containerization later, restore from a backup or follow DEPLOY-CLOUD-RUN.md.

