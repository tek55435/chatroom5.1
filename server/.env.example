# Copy this to server/.env and set your OpenAI server API key (do not commit real keys).
# For production on Cloud Run, prefer Secret Manager over .env.
OPENAI_API_KEY=sk-your-real-openai-api-key
PORT=3000
MODEL=gpt-4o-realtime-preview-2024-12-17
